availableAddresses:
  title: Available Addresses
  queries:
    azure:
      select: sum(`azure.network.virtualnetworks.availableAddresses`)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
