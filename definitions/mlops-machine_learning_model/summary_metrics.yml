totalInferences:
  goldenMetric: totalInferences
  title: "Total Inference"
  unit: REQUESTS_PER_MINUTE
  query:
    select: (uniqueCount(inference_id) / 30)
    from: InferenceData
    eventId: entity.guid
