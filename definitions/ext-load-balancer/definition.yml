domain: EXT
type: LOAD_BALANCER
synthesis:
  name: device_name
  identifier: device_name
  encodeIdentifierInGUID: true

  conditions:
  - attribute: provider
    value: kentik-netscaler

  tags:
    src_addr:
      entityTagName: device_ip

goldenTags:
- device_ip

dashboardTemplates:
  kentik/netscaler:
    template: netscaler-dashboard.json
