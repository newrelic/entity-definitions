requests:
  title: Successful request latency
  unit: COUNT
  queries:
    aws:
      select: average(aws.timestream.SuccessfulRequestLatency)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
magneticStoreRejectedRecordCount:
  title: Magnetic store rejected record count
  unit: COUNT
  queries:
    aws:
      select: average(aws.timestream.MagneticStoreRejectedRecordCount)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
magneticStoreRejectedUploadSystemFailures:
  title: Magnetic store rejected upload system failures
  unit: COUNT
  queries:
    aws:
      select: sum(aws.timestream.MagneticStoreRejectedUploadSystemFailures)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
magneticStorePendingRecordsLatency:
  title: Magnetic store pending records latency
  unit: COUNT
  queries:
    aws:
      select: sum(aws.timestream.MagneticStorePendingRecordsLatency)
      from: Metric
      eventId: entity.guid
      eventName: entity.name