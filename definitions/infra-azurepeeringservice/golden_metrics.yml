roundTripTime:    
  title: Round trip time
  unit: MS
  queries:
    azure:
      select: average(azure.peering.peeringservices.RoundTripTime)
      from: Metric
      eventId: entity.guid
      eventName: entity.name