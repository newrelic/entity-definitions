domain: EXT
type: BILLABLE_USER

synthesis:
  rules:
  - compositeIdentifier:
      separator: "/"
      attributes:
        - email
        - authDomain
    encodeIdentifierInGUID: true
    conditions:
    - attribute: eventType
      value: NrUsers
    tags:
      authDomain:
        multiValue: false
      billableType:
        multiValue: false
      name:
        multiValue: false

dashboardTemplates:
  newRelic:
    template: dashboard.json

configuration:
  entityExpirationTime: EIGHT_DAYS
  alertable: true
