domain: EXTERNAL
type: SERVICE
synthesis:
  rules:
    # DistributedTraceProcessSummary events
    - identifier: traceType
      name: traceType
      encodeIdentifierInGUID: true
      conditions:
        - attribute: eventType
          value: DistributedTraceProcessSummary

configuration:
  entityExpirationTime: EIGHT_DAYS
  alertable: false