domain: EXTERNAL
type: SERVICE

synthesis:
  rules:
    # DistributedTraceProcessSummary events
  - identifier: entity.guid
    name: entity.guid
    encodeIdentifierInGUID: true
    conditions:
    - attribute: traceType
      value: distributedTracing
#    - attribute: eventType
#      value: DistributedTraceProcessSummary

configuration:
  entityExpirationTime: EIGHT_DAYS
  alertable: false