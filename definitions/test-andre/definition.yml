domain: TEST
type: ANDRE

synthesis:
  rules:
    - identifier: someIdField
      encodeIdentifierInGUID: true
      name: someNameField
      conditions:
        - attribute: metricName
          prefix: andre_
  tags:
    someNamespace.someValue:

configuration:
  alertable: true
  entityExpirationTime: DAILY
