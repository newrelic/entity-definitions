domain: EXT
type: SERVICE

synthesis:
  identifier: service.name
  name: service.name
  encodeIdentifierInGUID: true

  conditions:
    - attribute: service.name

  tags:
    - k8s.cluster.name
    - k8s.namespace.name
    - k8s.pod.name
    - k8s.node.name
