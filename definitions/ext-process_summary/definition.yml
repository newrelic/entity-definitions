domain: EXT
type: PROCESS_SUMMARY
synthesis:
  rules:
    # DistributedTraceProcessSummary events
    - identifier: process_summary
      name: process_summary
      encodeIdentifierInGUID: true
      conditions:
        - attribute: eventType
          value: DistributedTraceProcessSummary

configuration:
  entityExpirationTime: EIGHT_DAYS
  alertable: true