depth_count:
  title: Depth
  unit: COUNT
  query:
    select: latest(ibmmq_queue_depth) AS 'Depth'

queue_uncommitted_messages:
  title: Uncommitted messages
  unit: COUNT
  query:
    select: latest(ibmmq_queue_uncommitted_messages) AS 'Uncommitted messages'

queue_expired_messages:
  title: Expired messages
  unit: COUNT
  query:
    select: latest(ibmmq_queue_expired_messages) AS 'Expired messages'
