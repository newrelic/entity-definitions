domain: INFRA
type: IBMMQ_MANAGER

synthesis:
  rules:
    - identifier: targetName
      name: targetName
      conditions:
        - attribute: metricName
          prefix: ibmmq_
      tags:
        clusterName:

configuration:
  entityExpirationTime: DAILY
  alertable: true
