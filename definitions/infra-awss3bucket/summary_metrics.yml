providerAccountName:
  tag:
    key: providerAccountName
  title: AWS Account
  unit: STRING
size:
  query:
    eventId: entityGuid
    select: average(provider.bucketSizeBytes.Average)
    from: DatastoreSample
  unit: BYTES
  title: Bucket size
objects:
  query:
    eventId: entityGuid
    select: average(provider.numberOfObjects.Average)
    from: DatastoreSample
  unit: COUNT
  title: Objects
