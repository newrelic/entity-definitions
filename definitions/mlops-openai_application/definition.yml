domain: MLOPS
type: OPENAI_APPLICATION
synthesis:
  rules:
    - identifier: applicationName
      name: applicationName
      encodeIdentifierInGUID: true
      conditions:
        - attribute: eventType
          value: OpenAICompletion
      tags:
          providerAccountId:
          providerAccountName:
          instrumentation.provider:
dashboardTemplates:
  newRelic:
    template: dashboard.json
configuration:
  entityExpirationTime: QUARTERLY
  alertable: true