  #  SELECT entityGuid (target), nodeName (source) FRom Span where nodeName is not null
  #  existing relationship
relationships:
  - provider: newrelic
    eventType: Span
    rootEntityNodeType: TARGET
    relationship: HOSTS
    relatedEntity:
      domain: EXT
      type: SERVICE
      domainId: nodeName
    conditions:
      - attribute: nodeName #verify attr exists

