domain: EXT
type: DATADOG_MYSQL

synthesis:
  name: dd.mysql
  identifier: dd.mysql
  encodeIdentifierInGUID: false
  conditions:
    - attribute: metricName
      prefix: mysql.

compositeMetrics:
  goldenMetrics:
    - golden_metrics.yml

configuration:
  entityExpirationTime: EIGHT_DAYS
  alertable: true
