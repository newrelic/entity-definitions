domain: INFRA
type: AWSLAMBDAFUNCTION
goldenTags:
- aws.awsRegion
- aws.region
- aws.accountId
- aws.memorySize
- aws.runtime
- aws.timeout
- aws.handler
- account
- label.Team
- label.team
- label.env
- label.environment

synthesis:
  rules:
    - identifier: faas.name
      name: faas.name
      encodeIdentifierInGUID: true
      conditions:
        - attribute: eventType
          value: Log
        - attribute: newrelic.source
          value: 'api.logs'

configuration:
  entityExpirationTime: DAILY
  alertable: true
