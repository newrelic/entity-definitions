latency:
  title: Latency
  unit: MS
  queries:
    azure:
      select: average(azure.cognitiveservices.accounts.Latency)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
totalEvents:
  title: Total events
  unit: COUNT
  queries:
    azure:
      select: sum(azure.cognitiveservices.accounts.TotalEvents)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
totalCalls:
  title: Total calls
  unit: COUNT
  queries:
    azure:
      select: sum(azure.cognitiveservices.accounts.TotalCalls)
      eventId: entity.guid
      eventName: entity.name
successfulCalls:
  title: Successful calls
  unit: COUNT
  queries:
    azure:
      select: sum(azure.cognitiveservices.accounts.SuccessfulCalls)
      eventId: entity.guid
      eventName: entity.name
serverErrors:
  title: Server errors
  unit: COUNT
  queries:
    azure:
      select: sum(azure.cognitiveservices.accounts.ServerErrors)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
blockedCalls:
  title: Blocked calls
  unit: COUNT
  queries:
    azure:
      select: sum(azure.cognitiveservices.accounts.BlockedCalls)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
clientErrors:
  title: Client errors
  unit: COUNT
  queries:
    azure:
      select: sum(azure.cognitiveservices.accounts.ClientErrors)
      from: Metric
      eventId: entity.guid
      eventName: entity.name