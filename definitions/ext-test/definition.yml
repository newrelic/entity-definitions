domain: EXT
type: TEST
synthesis:
  rules:
  - identifier: test.name
    name: test.name
    encodeIdentifierInGUID: true
    conditions:
    - attribute: metricName
      prefix: test.

configuration:
  entityExpirationTime: EIGHT_DAYS
  alertable: true
