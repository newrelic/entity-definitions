apiserverAuditEventTotal:
  title: Number of apiserver audit events
  unit: COUNT
  query:
    select: sum(apiserver_audit_event_total)
  kind: SUM
apiserverRequestCount:
  title: Number of apiserver requests
  unit: COUNT
  query:
    select: sum(apiserver_request_count)
  kind: SUM
