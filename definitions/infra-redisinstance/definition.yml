domain: INFRA
type: REDISINSTANCE
goldenTags:
- redis.port
- redis.hostname
- redis.version
- redis.clusterRole
compositeMetrics:
  goldenMetrics:
  - golden_metrics.yml
  summaryMetrics:
  - summary_metrics.yml
configuration:
  entityExpirationTime: DAILY
  alertable: true
