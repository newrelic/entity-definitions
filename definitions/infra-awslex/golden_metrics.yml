requests:
  title: Total Request count
  unit: COUNT
  queries:
    aws:
      select: sum(aws.lex.RuntimeRequestCount)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
runtimeSystemErrors:
  title: Runtime System Errors count
  unit: COUNT
  queries:
    aws:
      select: sum(aws.lex.RuntimeSystemErrors)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
runtimeUserErrors:
  title: Runtime User Errors count
  unit: COUNT
  queries:
    aws:
      select: sum(aws.lex.RuntimeUserErrors)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
runtimeThrottledEvents:
  title: Throttled Events count
  unit: COUNT
  queries:
    aws:
      select: sum(aws.lex.RuntimeThrottledEvents)
      from: Metric
      eventId: entity.guid
      eventName: entity.name