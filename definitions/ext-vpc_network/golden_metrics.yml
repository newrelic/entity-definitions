sources:
  title: Unique Sources
  unit: COUNT
  queries:
    kentik/vpc-flow-events:
      select: uniqueCount(src_endpoint)
      from: KFlow
      where: "provider = 'kentik-vpc'"

destinations:
  title: Unique Destinations
  unit: COUNT
  queries:
    kentik/vpc-flow-events:
      select: uniqueCount(dst_endpoint)
      from: KFlow
      where: "provider = 'kentik-vpc'"
