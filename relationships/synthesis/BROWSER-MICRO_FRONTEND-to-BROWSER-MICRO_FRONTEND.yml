relationships:
  - name: BrowserApplicationContainsMicrofrontend
    version: "1"
    origins:
      - BrowserMonitoring
    conditions:
      - attribute: appId
        present: true
      - attribute: appName
        present: true
    relationship:
      expires: PT72H
      relationshipType: CONTAINS 
      source:
        extractGuid:
          attribute: entityGuid
      target:
        extractGuid:
          attribute: entity.guid # todo: this isn't quite right