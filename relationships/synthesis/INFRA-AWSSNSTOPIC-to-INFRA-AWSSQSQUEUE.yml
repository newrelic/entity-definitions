relationships:
  - name: awsSnsTopicProducesSqsQueue
    version: "1"
    origins:
      - AWS Integration
    conditions:
      - attribute: eventType
        anyOf: [ "Metric" ]
      - attribute: entity.type
        anyOf: [ "AWSSQSQUEUE", "AWS_SQS_QUEUE" ]
    relationship:
      expires: P75M
      relationshipType: PRODUCES
      source:
        lookupGuid:
          candidateCategory: AWSSNSTOPIC
          fields:
            - field: awsAccountId
              attribute: aws.accountId
            - field: awsRegion
              attribute: aws.region
      target:
        extractGuid:
          attribute: entity.guid
          entityType:
            value: AWSSQSQUEUE

  - name: awsSnsTopicTriggersLambdaFunction
    version: "1"
    origins:
      - AWS Integration
    conditions:
      - attribute: eventType
        anyOf: [ "Metric" ]
      - attribute: entity.type
        anyOf: [ "AWSLAMBDAFUNCTION", "AWS_LAMBDA_FUNCTION" ]
    relationship:
      expires: P75M
      relationshipType: TRIGGERS
      source:
        lookupGuid:
          candidateCategory: AWSSNSTOPIC
          fields:
            - field: awsAccountId
              attribute: aws.accountId
            - field: awsRegion
              attribute: aws.region
      target:
        extractGuid:
          attribute: entity.guid
          entityType:
            value: AWSLAMBDAFUNCTION

  - name: awsSnsTopicTriggersSnsTopicTriggers
    version: "1"
    origins:
      - AWS Integration
    conditions:
      - attribute: eventType
        anyOf: [ "Metric" ]
      - attribute: entity.type
        anyOf: [ "AWSSNSTOPIC", "AWS_SNS_TOPIC" ]
    relationship:
      expires: P75M
      relationshipType: TRIGGERS
      source:
        lookupGuid:
          candidateCategory: AWSSNSTOPIC
          fields:
            - field: awsAccountId
              attribute: aws.accountId
            - field: awsRegion
              attribute: aws.region
      target:
        extractGuid:
          attribute: entity.guid
          entityType:
            value: AWSSNSTOPIC
