relationships:
    # Relationship for OTEL Kafka broker metrics in K8s pods
  - name: k8sPodHostsKafkaBroker1
    version: "1"
    origins:
      - OpenTelemetry
    conditions:
      - attribute: eventType
        anyOf: [ "Metric" ]
      - attribute: metricName
        startsWith: kafka
      - attribute: instrumentation.provider
        anyOf: [ "opentelemetry" ]
      - attribute: k8s.cluster.name
        present: true
      - attribute: k8s.namespace.name
        present: true
      - attribute: k8s.pod.name
        present: true
    relationship:
      expires: PT75M
      relationshipType: HOSTS
      source:
        buildGuid:
          account:
            lookup: true
          domain:
            value: INFRA
          type:
            value: KUBERNETES_POD
          identifier:
            fragments:
              - attribute: k8s.cluster.name
              - value: ":"
              - attribute: k8s.namespace.name
              - value: ":"
              - attribute: k8s.pod.name
            hashAlgorithm: FARM_HASH
      target:
        extractGuid:
          attribute: entity.guid
          entityType:
            value: KAFKABROKER

  # Experimental relationship for OTEL Kafka broker metrics in K8s pods (alternative to above)
  - name: k8sPodHostsKafkaBroker2
    version: "1"
    origins:
      - OpenTelemetry
    conditions:
      - attribute: eventType
        anyOf: [ "Metric" ]
      - attribute: metricName
        startsWith: kafka
      - attribute: instrumentation.provider
        anyOf: [ "opentelemetry" ]
      - attribute: kafka.cluster.name
        present: true
      - attribute: k8s.namespace.name
        present: true
      - attribute: k8s.pod.name
        present: true
    relationship:
      expires: PT75M
      relationshipType: HOSTS
      source:
        buildGuid:
          account:
            lookup: true
          domain:
            value: INFRA
          type:
            value: KUBERNETES_POD
          identifier:
            fragments:
              - attribute: kafka.cluster.name
              - value: ":"
              - attribute: k8s.namespace.name
              - value: ":"
              - attribute: k8s.pod.name
            hashAlgorithm: FARM_HASH
      target:
        extractGuid:
          attribute: entity.guid
          entityType:
            value: KAFKABROKER

  # Matches the Pod entity by tags instead of reconstructing its identifier hash.
  - name: k8sPodHostsKafkaBrokerCandidate
    version: "1"
    origins:
      - OpenTelemetry
    conditions:
      - attribute: eventType
        anyOf: [ "Metric" ]
      - attribute: metricName
        startsWith: kafka
      - attribute: instrumentation.provider
        anyOf: [ "opentelemetry" ]
      - attribute: kafka.cluster.name
        present: true
      - attribute: k8s.namespace.name
        present: true
      - attribute: k8s.pod.name
        present: true
    relationship:
      expires: PT75M
      relationshipType: HOSTS
      source:
        lookupGuid:
          candidateCategory: KUBERNETES_POD
          fields:
            - field: k8s.pod.name
              attribute: k8s.pod.name
            - field: k8s.namespace.name
              attribute: k8s.namespace.name
            - field: k8s.cluster.name
              attribute: kafka.cluster.name
      target:
        extractGuid:
          attribute: entity.guid
          entityType:
            value: KAFKABROKER
