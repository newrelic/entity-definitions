relationships:
  - name: k8sPodContainsExtArgocd
    version: "1"
    origins:
      - Kubernetes Integration
    conditions:
      - attribute: eventType
        anyOf: ["Metric"]
      - attribute: entity.type
        anyOf: ["ARGOCD"]
    relationship:
      expires: P75M
      relationshipType: CONTAINS
      source:
        extractGuid:
          attribute: entity.guid
      target:
        lookupGuid:
          candidateCategory: ARGOCD
          fields:
            - field: k8s.podName # podName of ext-argocd
              attribute: k8s.podName # podName of pod
            - field: k8s.namespaceName # namespace of ext-argocd
              attribute: k8s.namespaceName # namespace of pod
            - field: k8s.clusterName # cluster of ext-argocd
              attribute: k8s.clusterName # cluster of pod
