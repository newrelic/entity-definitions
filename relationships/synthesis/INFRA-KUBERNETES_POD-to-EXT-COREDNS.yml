relationships:
  - name: k8sPodContainsExtCoredns
    version: "1"
    origins:
      - Kubernetes Integration
    conditions:
      - attribute: eventType
        anyOf: ["Metric"]
      - attribute: entity.type
        anyOf: ["KUBERNETES_POD"]
    relationship:
      expires: P75M
      relationshipType: CONTAINS
      source:
        extractGuid:
          attribute: entity.guid
      target:
        lookupGuid:
          candidateCategory: COREDNS
          fields:
            - field: k8s.clusterName # cluster of ext-coredns
              attribute: cluster_name # cluster of pod
            - field: k8s.namespaceName # namespace of ext-coredns
              attribute: namespace # namespace of pod
            - field: k8s.podName # podName of ext-coredns
              attribute: pod # podName of pod
