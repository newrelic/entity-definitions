partitionsWithNonPreferredLeader:
  title: Partitions with non-preferred leader
  unit: COUNT
  queries:
    newRelic:
      select: average(topic.partitionsWithNonPreferredLeader)
      from: KafkaTopicSample
      eventId: entityGuid
      eventName: entityName
respondMetaData:
  title: Respond metadata
  unit: COUNT
  queries:
    newRelic:
      select: average(topic.respondMetaData)
      from: KafkaTopicSample
      eventId: entityGuid
      eventName: entityName
retentionSizeOrTime:
  title: Retention size or time
  unit: COUNT
  queries:
    newRelic:
      select: average(topic.retentionSizeOrTime)
      from: KafkaTopicSample
      eventId: entityGuid
      eventName: entityName
underReplicatedPartitions:
  queries:
    newRelic:
      select: average(topic.underReplicatedPartitions)
      from: KafkaTopicSample
      eventId: entityGuid
      eventName: entityName
  unit: COUNT
  title: Under replicated partitions
