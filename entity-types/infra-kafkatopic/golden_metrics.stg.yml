
partitionsWithNonPreferredLeader:
  title: Partitions with non-preferred leader
  unit: COUNT
  queries:
    newRelic:
      select: average(topic.partitionsWithNonPreferredLeader)
      from: KafkaTopicSample
      eventId: entityGuid
      eventName: entityName
underReplicatedPartitions:
  queries:
    newRelic:
      select: latest(topic.underReplicatedPartitions)
      from: KafkaTopicSample
      eventId: entityGuid
      eventName: entityName
    opentelemetry:
      select: latest(kafka.partition.replicas)
      from: Metric
      where: metricName = 'kafka.partition.replicas'
      eventId: entity.guid
      eventName: entity.name
  unit: COUNT
  title: Under replicated partitions