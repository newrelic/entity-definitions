domain: INFRA
type: COUCHBASEBUCKET

synthesis:
  rules:
    - ruleName: CouchbaseBucketFromDB
      identifier: entityId
      name: entityName
      conditions:
        - attribute: event_type
          value: 'CouchbaseBucketSample'

configuration:
  entityExpirationTime: DAILY
  alertable: true

dashboardTemplates:
  newRelic:
    template: newrelic_dashboard.json

ownership:
  primaryOwner:
    teamName: "Database Integrations"