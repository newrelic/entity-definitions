domain: INFRA
type: KUBERNETES_APISERVER
goldenTags:
- label.kubernetes.io/os
configuration:
  entityExpirationTime: DAILY
  alertable: true
dashboardTemplates:
  newRelic:
    template: dashboard.json
