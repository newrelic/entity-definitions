domain: TVS
type: SCREENDEVICE
synthesis:
  rules:
  - ruleName: tvs_screendevice_screenDevice
    identifier: screenDevice
    encodeIdentifierInGUID: true
    name: screenDevice
    conditions:
      - attribute: screenDevice
        present: true
      - attribute: metricName
        prefix: screenDevice_

ownership:
  primaryOwner:
    teamName: "no owner"
