bgpSessionsUp:
  title: BGP Sessions Up
  unit: COUNT
  queries:
    gcp:
      select: latest(`gcp.router.bgp_sessions_up_count`)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
bgpSessionsDown:
  title: BGP Sessions Down
  unit: COUNT
  queries:
    gcp:
      select: latest(`gcp.router.bgp_sessions_down_count`)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
sentRoutesCount:
  title: Sent Routes Count
  unit: COUNT
  queries:
    gcp:
      select: latest(`gcp.router.sent_routes_count`)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
