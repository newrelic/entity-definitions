domain: EXT
type: DELL_XTREMIO

synthesis:
  ruleName: dell_xtremio_clusterci
  name: clusterCI
  identifier: clusterCI
  encodeIdentifierInGUID: true
  conditions:
  - attribute: eventType
    prefix: XIO

dashboardTemplates:
  newRelic:
    template: dashboard.json

configuration:
  entityExpirationTime: EIGHT_DAYS
  alertable: true

ownership:
  primaryOwner:
    teamName: "no owner"
