phoneVerificationCount:
  title: Phone Verification Count
  unit: COUNT
  queries:
    gcp:
      select: sum(gcp.firebaseauth.phone_auth.phone_verification_count)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
