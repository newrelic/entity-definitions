domain: INFRA
type: GCPAPIGEEPROXY

synthesis:
  rules:
  - identifier: entityId
    name: displayName
    encodeIdentifierInGUID: true
    conditions:
    - attribute: eventType
      value: GcpApigeeProxySample
    - attribute: provider
      value: GcpApigeeProxy
    tags:
      gcp.organization:
      gcp.environment:
      gcp.projectId:
      gcp.proxyName:
      gcp.revision:
      label.*:

configuration:
  entityExpirationTime: DAILY
  alertable: true

goldenTags:
- gcp.organization
- gcp.environment
- gcp.proxyName

ownership:
  primaryOwner:
    teamName: "Cloud Integrations"
