yellowResources:
  title: Yellow Resources
  unit: COUNT
  queries:
    aws:
      select: latest(aws.trustedadvisor.YellowResources)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
redResources:
  title: Red Resources
  unit: COUNT
  queries:
    aws:
      select: latest(aws.trustedadvisor.RedResources)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
