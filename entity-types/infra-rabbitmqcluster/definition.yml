domain: INFRA
type: RABBITMQCLUSTER
goldenTags:
- account
configuration:
  entityExpirationTime: DAILY
  alertable: true

# this entity type is EOL since July 2022 - https://github.com/newrelic/nri-rabbitmq/pull/89 

dashboardTemplates:
  # This should match the entity created from the ohi in the infra pipeline
  newRelic:
    template: newrelic_dashboard.json

ownership:
  primaryOwner:
    teamName: "OnHost Agent and Integrations"
