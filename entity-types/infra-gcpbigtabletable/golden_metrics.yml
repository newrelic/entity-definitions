replicationRequestLatenciesMs:
  title: Replication request latencies (ms)
  unit: MS
  queries:
    gcp:
      select: percentile(gcp.bigtable.replication.latency, 95)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
    gcpSample:
      select: percentile(replication.Latency, 95)
      from: GcpBigtableTableSample
      eventId: entityGuid
      eventName: entityName
serverRequestsFailed:
  title: Server requests failed
  unit: COUNT
  queries:
    gcp:
      select: sum(gcp.bigtable.server.error_count)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
    gcpSample:
      select: sum(server.Error)
      from: GcpBigtableTableSample
      eventId: entityGuid
      eventName: entityName
serverRequests:
  title: Server requests
  unit: COUNT
  queries:
    gcp:
      select: sum(gcp.bigtable.server.request_count)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
    gcpSample:
      select: sum(server.Request)
      from: GcpBigtableTableSample
      eventId: entityGuid
      eventName: entityName
uncompressedBytesOfResponseData:
  title: Uncompressed bytes of response data
  unit: BYTES
  queries:
    gcp:
      select: sum(gcp.bigtable.server.sent_bytes_count)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
    gcpSample:
      select: sum(server.SentBytes)
      from: GcpBigtableTableSample
      eventId: entityGuid
      eventName: entityName
