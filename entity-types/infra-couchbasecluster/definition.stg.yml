domain: INFRA
type: COUCHBASECLUSTER

synthesis:
  rules:
    - ruleName: CouchbaseClusterFromDB
      identifier: entityId
      name: entityName
      conditions:
        - attribute: event_type
          value: 'CouchbaseClusterSample'
configuration:
  entityExpirationTime: DAILY
  alertable: true

dashboardTemplates:
  newRelic:
    template: newrelic_dashboard.json

ownership:
  primaryOwner:
    teamName: "no owner"
