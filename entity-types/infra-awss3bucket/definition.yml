domain: INFRA
type: AWSS3BUCKET

goldenTags:
  - aws.accountId
  - aws.availabilityZone
  - aws.awsRegion
  - aws.region
  - aws.s3.bucketUrl
dashboardTemplates:
  newRelic:
    template: dashboard.json

configuration:
  entityExpirationTime: DAILY
  alertable: true
