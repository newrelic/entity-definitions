domain: NR1
type: REPOSITORY

synthesis:
  rules:
    # For New Relic only - telemetry with nrOnlyRepositoryUrl attribute
  - identifier: nrOnlyRepositoryUrl
    name: repositoryUrl
    encodeIdentifierInGUID: true
    conditions:
    - attribute: nrOnlyRepositoryUrl

configuration:
  entityExpirationTime: QUARTERLY
  alertable: true