domain: NR1
type: REPOSITORY

synthesis:
  rules:
    # telemetry with repositoryUrl attribute
  - identifier: repositoryUrl
    name: repositoryUrl
    encodeIdentifierInGUID: true
    conditions:
    - attribute: repositoryUrl

configuration:
  entityExpirationTime: MANUAL
  alertable: true