# -- Mandatory fields --
domain: EXT
type: TINYPROXY

synthesis:
  # -- Mandatory fields --
  name: targetName
  identifier: targetName

  conditions:
  - attribute: metricName
    prefix: tinyproxy_

  tags:
    instrumentation.name:

dashboardTemplates:
  newRelic:
    template: dashboard.json

goldenTags:
- instrumentation.name
