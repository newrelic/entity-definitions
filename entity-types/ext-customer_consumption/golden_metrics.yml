dataIngest:
  title: GB Ingested
  unit: BYTES
  queries:
    newRelic:
      select: latest(consumption_bytes)
fullUsers:
  title: Full Users
  unit: COUNT
  queries:
    newRelic:
      select: latest(consumption_fullUsers)
coreUsers:
  title: Core Users
  unit: COUNT
  queries:
    newRelic:
      select: latest(consumption_coreUsers)
basicUsers:
  title: Full Users
  unit: COUNT
  queries:
    newRelic:
      select: latest(consumption_basicUsers)
