domain: EXT
type: ZERTO

synthesis:
  rules:
  - ruleName: ext_zerto_clusterCI
    identifier: clusterCI
    name: clusterCI
    encodeIdentifierInGUID: true
    conditions:
    - attribute: eventType
      prefix: zerto

dashboardTemplates:
  newRelic:
    template: dashboard.json

configuration:
  entityExpirationTime: EIGHT_DAYS
  alertable: true

ownership:
  primaryOwner:
    teamName: "no owner"
