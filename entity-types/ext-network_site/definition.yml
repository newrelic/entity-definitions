domain: EXT
type: NETWORK_SITE
synthesis:
  rules:
  # Network Sites via network-data-processor
  - ruleName: ext_network_site_network_site_id
    identifier: network.site.id
    name: network.site.name
    encodeIdentifierInGUID: true
    conditions:
    - attribute: instrumentation.provider
      value: network-data-processor
    - attribute: network.device.type
      value: site
    tags:
      network.vendor:
      network.org.id:
      network.site.address:
      network.site.country:
      network.site.timezone:
    prefixedTags:
      tags.:
        ttl: PT4H

goldenTags:
- network.vendor
- network.org.id

ownership:
  primaryOwner:
    teamName: "no owner"
