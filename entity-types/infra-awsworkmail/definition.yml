domain: INFRA
type: AWSWORKMAIL
goldenTags:
  - aws.accountId
  - aws.environmentName
  - aws.region
  - aws.workmail.OrganizationId
dashboardTemplates:
  newRelic:
    template: dashboard.json
    filterNRQLByEntityID: true
configuration:
  entityExpirationTime: DAILY
  alertable: true

ownership:
  primaryOwner:
    teamName: "no owner"
