putMessagesRecordsCount:
  title: Put Messages Records Count
  unit: COUNT
  queries:
    oci:
      select: sum(oci.streaming.put.messages.records.count)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
