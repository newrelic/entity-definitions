offsetLag:
  title: Consumer Offset Lag
  unit: COUNT
  queries:
    gcp:
      select: average(gcp.managedkafka.offset_lag)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
