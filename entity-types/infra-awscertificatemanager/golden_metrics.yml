daysToExpiry:
  title: Days To Expiry
  unit: COUNT
  queries:
    aws:
      select: latest(aws.certificatemanager.DaysToExpiry)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
