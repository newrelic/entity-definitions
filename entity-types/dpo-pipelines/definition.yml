domain: DPO
type: PIPELINES
synthesis:
  rules:
  - compositeIdentifier:
        separator: ":"
        attributes:
          - pipeline.namespace
          - pipeline.name
    name: pipeline.name
    encodeIdentifierInGUID: true
    conditions:
    - attribute: pipeline.name
    tags:
      pipeline.namespace:
      pipeline.name:
configuration:
  entityExpirationTime: MANUAL
  alertable: true