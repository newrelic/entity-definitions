domain: EXT
type: SITE

synthesis:
    # telemetry with SiteID attribute
    ruleName: ext_site_id
    name: SiteID
    identifier: SiteID
    encodeIdentifierInGUID: true
    conditions:
    - attribute: SiteID

dashboardTemplates:
  newRelic:
    template: dashboard.json

configuration:
  entityExpirationTime: EIGHT_DAYS
  alertable: true

ownership:
  primaryOwner:
    teamName: "no owner"
