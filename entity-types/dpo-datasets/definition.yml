domain: DPO
type: DATASETS
synthesis:
  rules:
  - compositeIdentifier:
        separator: ":"
        attributes:
          - dataset.namespace
          - dataset.name
    name: dataset.name
    encodeIdentifierInGUID: true
    conditions:
    - attribute: dataset.name
    tags:
      dataset.namespace:
      dataset.name:
configuration:
  entityExpirationTime: MANUAL
  alertable: true