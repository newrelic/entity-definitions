domain: EXT
type: DELL_AVAMAR

synthesis:
  rules:
  - ruleName: ext_dell_avamar_clusterCI
    identifier: clusterCI
    name: clusterCI
    encodeIdentifierInGUID: true
    conditions:
    - attribute: eventType
      prefix: avamar

dashboardTemplates:
  newRelic:
    template: dashboard.json

configuration:
  entityExpirationTime: EIGHT_DAYS
  alertable: true

ownership:
  primaryOwner:
    teamName: "no owner"
