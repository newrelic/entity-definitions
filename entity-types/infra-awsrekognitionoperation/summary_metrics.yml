providerAccountName:
  title: AWS account
  unit: STRING
  tag:
    key: providerAccountName
region:
  title: Region
  unit: STRING
  tag:
    key: aws.awsRegion
requests:
  goldenMetric: requests
  title: Total Request count
  unit: COUNT
userError:
  goldenMetric: userError
  title: User Error count
  unit: COUNT
serverError:
  goldenMetric: serverError
  title: Server Error count
  unit: COUNT
throttledCount:
  goldenMetric: throttledCount
  title: Throttled count
  unit: COUNT