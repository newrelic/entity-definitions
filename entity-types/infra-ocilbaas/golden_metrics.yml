activeConnections:
  title: Active Connections
  unit: COUNT
  queries:
    oci:
      select: sum(oci.lbaas.active.connections)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
backendServers:
  title: Backend Servers
  unit: COUNT
  queries:
    oci:
      select: sum(oci.lbaas.backend.servers)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
bytesReceived:
  title: Bytes Received
  unit: BYTES_PER_SECOND
  queries:
    oci:
      select: sum(oci.lbaas.bytes.received)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
bytesSent:
  title: Bytes Sent
  unit: BYTES_PER_SECOND
  queries:
    oci:
      select: sum(oci.lbaas.bytes.sent)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
closedConnections:
  title: Closed Connections
  unit: COUNT
  queries:
    oci:
      select: sum(oci.lbaas.closed.connections)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
unhealthyBackendServers:
  title: Unhealthy Backend Servers
  unit: COUNT
  queries:
    oci:
      select: sum(oci.lbaas.unhealthy.backend.servers)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
acceptedConnections:
  title: Accepted Connections
  unit: COUNT
  queries:
    oci:
      select: sum(oci.lbaas.accepted.connections)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
