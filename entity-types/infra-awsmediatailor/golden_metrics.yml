adNotReadyCount:
  title: Ad not ready count
  unit: COUNT
  queries:
    aws:
      select: sum(aws.mediatailor.AdNotReady)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
requestCount:
  title: Channel request count
  unit: COUNT
  queries:
    aws:
      select: sum(aws.mediatailor.RequestCount)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
getManifestErrorsCount:
  title: Get manifest errors count
  unit: COUNT
  queries:
    aws:
      select: sum(aws.mediatailor.GetManifest.Errors)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
adDecisionServerErrorsCount:
  title: Ad decision server errors count
  unit: COUNT
  queries:
    aws:
      select: sum(aws.mediatailor.AdDecisionServer.Errors)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
originErrors:
  title: Origin errors
  unit: COUNT
  queries:
    aws:
      select: sum(aws.mediatailor.Origin.Errors)
      from: Metric
      eventId: entity.guid
      eventName: entity.name