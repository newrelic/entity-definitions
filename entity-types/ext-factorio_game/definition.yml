domain: EXT
type: FACTORIO_GAME

synthesis:
  rules:
    - ruleName: ext_factorio_game_game_name
      identifier: game_name
      name: game_name
      encodeIdentifierInGUID: true
      conditions:
        - attribute: metricName
          prefix: factorio_


dashboardTemplates:
  newRelic:
    template: dashboard.json

configuration:
  entityExpirationTime: FOUR_HOURS
  alertable: true

ownership:
  primaryOwner:
    teamName: "NewRelic Community"
