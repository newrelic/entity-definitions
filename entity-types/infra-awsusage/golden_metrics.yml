callCount:
  title: Call Count
  unit: COUNT
  queries:
    aws:
      select: sum(aws.usage.CallCount)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
errorCount:
  title: Error Count
  unit: COUNT
  queries:
    aws:
      select: sum(aws.usage.ErrorCount)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
resourceCount:
  title: Resource Count
  unit: COUNT
  queries:
    aws:
      select: sum(aws.usage.ResourceCount)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
throttleCount:
  title: Throttle Count
  unit: COUNT
  queries:
    aws:
      select: sum(aws.usage.ThrottleCount)
      from: Metric
      eventId: entity.guid
      eventName: entity.name
