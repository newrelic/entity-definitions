domain: EXT
type: TESTCOMPOSITE

synthesis:
  ruleName: ext_testcomposite_id_stg
  identifier: service.composite.id
  compositeName:
    fragments:
      - attribute: 'service.composite.name'
      - value: '-'
      - attribute: 'service.composite.host.id'
      - value: '-composed'

  conditions:
    - attribute: service.composite.id
    - attribute: service.composite.name
    - attribute: service.composite.host.id

  tags:
    service.composite.container.name:

configuration:
  entityExpirationTime: FOUR_HOURS
  alertable: true

ownership:
  primaryOwner:
    teamName: "no owner"