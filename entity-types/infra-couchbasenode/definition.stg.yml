domain: INFRA
type: COUCHBASENODE

synthesis:
  rules:
    - ruleName: CouchbaseNodeFromDB
      identifier: entityId
      name: entityName
      conditions:
        - attribute: event_type
          value: 'CouchbaseNodeSample'
configuration:
  entityExpirationTime: DAILY
  alertable: true

dashboardTemplates:
  newRelic:
    template: newrelic_dashboard.json

ownership:
  primaryOwner:
    teamName: "Database Integrations"
