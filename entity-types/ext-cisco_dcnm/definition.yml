domain: EXT
type: CISCO_DCNM
synthesis:
  rules:
    - ruleName: ext_cisco_dcnm_clusterCI
      identifier: clusterCI
      name: clusterCI
      encodeIdentifierInGUID: true
      conditions:
        - attribute: eventType
          prefix: dcnm
dashboardTemplates:
  newRelic:
    template: dashboard.json
configuration:
  entityExpirationTime: EIGHT_DAYS
  alertable: true

ownership:
  primaryOwner:
    teamName: "no owner"
