domain: DPO
type: JOBS
synthesis:
  rules:
  - compositeIdentifier:
        separator: ":"
        attributes:
          - job.namespace
          - job.name
    name: job.name
    encodeIdentifierInGUID: true
    conditions:
    - attribute: job.name
    tags:
      job.namespace:
      job.name:
      pipeline.name:
configuration:
  entityExpirationTime: MANUAL
  alertable: true