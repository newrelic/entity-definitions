domain: INFRA
type: GCPCLOUDRUNSERVICE

synthesis:
  rules:
  - identifier: entityId
    name: displayName
    encodeIdentifierInGUID: true
    conditions:
    - attribute: eventType
      value: GcpCloudRunServiceSample
    - attribute: provider
      value: GcpCloudRunService
    tags:
      gcp.region:
      gcp.projectId:
      gcp.serviceName:
      gcp.revisionName:
      label.*:

configuration:
  entityExpirationTime: DAILY
  alertable: true

goldenTags:
- gcp.region
- gcp.projectId
- gcp.serviceName

ownership:
  primaryOwner:
    teamName: "Cloud Integrations"
