summary_metrics:
  - name: "Latest Service Status"
    unit: "BOOLEAN"
    event_types:
      - Metric
    queries:
      select: "latest(service_status)"
      from: Metric
      where: "hostname = '{{ entity.hostname }}'"

